// Objeto Math

// funciones mátematicas
//let resultado = Math.PI;
let resultado;

resultado = Math.PI;
resultado = Math.round(2.6); //redondear hacia el entero mas cercano
resultado = Math.ceil(2.2); // siempre redondear hacia arriba
resultado = Math.floor(2.9); //siempre redondear hacia abajo
resultado = Math.sqrt(144); //raiz cuadrada
resultado = Math.abs(-200); //valor absoluto
resultado = Math.min(3, 5, 1, 8, 2, 10); //valor mínimo de un listado
resultado = Math.max(3, 5, 1, 8, 2, 10); //valor máximo de un listado
resultado = Math.random(); //número aleatorio
resultado = Math.floor(Math.random()*10); //número aleatorio entero

console.log(resultado);